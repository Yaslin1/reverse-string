//reverse a string

let pups = "My pups are the best!";

function pupsReverse (rev) {
    let pupString = "";
    for(i = 0; i < 9; i++); {
        pupString = i[0] + pups;
    }
    return pupString
}

console.log (pupsReverse(pups))